package osu.framework.allocation;

/**
 * Marker interface for types that can have dependencies injected.
 */
public interface IDependencyInjectionCandidate {
    // Marker interface - no methods required
}
